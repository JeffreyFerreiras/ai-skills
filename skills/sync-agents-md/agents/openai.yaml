interface:
  display_name: "Sync AGENTS.md"
  short_description: "Audit and reconcile agent instructions"
  default_prompt: "Use $sync-agents-md to synchronize the canonical AGENTS.md into the requested instruction files, preserving local guidance and backups."
