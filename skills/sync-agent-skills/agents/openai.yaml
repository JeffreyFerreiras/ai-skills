interface:
  display_name: "Sync Agent Skills"
  short_description: "Sync user-profile skills without repository copies"
  default_prompt: "Use $sync-agent-skills to audit and sync my user-profile agent skills, leaving repository and worktree copies untouched."
