# ParcelPilot

ParcelPilot calculates shipping quotes and dispatches shipments. Application code owns workflows, domain code owns shipping contracts, and infrastructure provides replaceable implementations.

Run the executable contract checks with:

```text
dotnet run --project tests/ParcelPilot.ContractChecks
```
